<section class="contact" id="contact">
       <div class="container">
            @include('contacts.title')

            <div class="row">
                @include('contacts.form')
                @include('contacts.address')
            </div>

       </div>
</section>